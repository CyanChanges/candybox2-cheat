// We can't use this export default class directly, we have to use a daughter class
export default class EnigmaAnswer{
    // Constructor
    constructor(){
        
    }
    
    // Public methods
    public isRight(answer: string): boolean{
        return false;
    }
}
